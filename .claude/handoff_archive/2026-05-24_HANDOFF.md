# HANDOFF - Lumina Deck v1 M1 Feature Gates

Branch: `feat/v1-feature-gates`

## What changed
- Added `packages/shared/src/feature-gates.ts` as the single TS source of truth for feature availability (`FEATURE_GATES`).
- Derived shared/mobile boolean gates in `FREE_LIMITS` and `PRO_LIMITS` from `FEATURE_GATES`, including explicit `textInput` and `trackpad` flags.
- Derived the protocol `CompanionCapability` type from enabled feature gates via `COMPANION_CAPABILITIES`.
- Repointed shared action-form `free` flags at `FEATURE_GATES` so editor metadata does not drift.
- Reconciled/commented Rust `ADVERTISED_CAPABILITIES` as the cross-language mirror of `COMPANION_CAPABILITIES`.
- Kept current policy unchanged: OBS remains off (`free: false`, `pro: false`); no action types were added.

## Consistency test
`packages/shared/src/feature-gates.test.ts` asserts:
- `FREE_LIMITS` / `PRO_LIMITS` boolean flags match `FEATURE_GATES`.
- Action form `free` flags match `FEATURE_GATES`.
- Protocol capabilities are generated from enabled gates, include `text_input` + `trackpad`, and exclude disabled `obs`.
- Rust `apps/companion/src-tauri/src/server.rs` `ADVERTISED_CAPABILITIES` exactly matches TS `COMPANION_CAPABILITIES`.

## Verification run
- `npx.cmd tsc --noEmit -p packages/shared/tsconfig.json` - PASS
- `npx.cmd vitest run --root packages/shared` - PASS (129 tests)
- `npx.cmd turbo run test --force` - PASS (shared test package, 129 tests; this sandbox needed a temporary local `pnpm.cmd` shim because pnpm/corepack was unavailable)
- `cd apps/companion/src-tauri && cargo check` - PASS (14 pre-existing warnings)

## Commit status
- Commit was attempted but blocked by local `.git` ACL/sandbox permissions: `fatal: Unable to create 'C:/Dev/LuminaDeck/.git/index.lock': Permission denied`.
- Specific intended M1 files to stage: `HANDOFF.md`, `packages/shared/src/feature-gates.ts`, `packages/shared/src/feature-gates.test.ts`, `packages/shared/src/types.ts`, `packages/shared/src/protocol.ts`, `packages/shared/src/action-forms.ts`, `packages/shared/src/index.ts`, `apps/companion/src-tauri/src/server.rs`.

## Notes
- No M2 work started.
- Python/graphify rebuild was not run because `python3` is not on PATH in this sandbox.
